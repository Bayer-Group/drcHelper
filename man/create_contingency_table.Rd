% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quantal.R
\name{create_contingency_table}
\alias{create_contingency_table}
\title{Create Contingency Table from Count Data}
\usage{
create_contingency_table(
  data,
  group_col,
  success_col,
  failure_col,
  prefix = NULL,
  col_names = NULL
)
}
\arguments{
\item{data}{A data frame containing the data}

\item{group_col}{String name of the column to group by (e.g., "dose")}

\item{success_col}{String name of the column containing success counts (e.g., "alive")}

\item{failure_col}{String name of the column containing failure counts (e.g., "dead")}

\item{prefix}{String prefix to add to row names (default: the group column name followed by "_")}

\item{col_names}{Character vector of length 2 for column names (default: c("success", "failure"))}
}
\value{
A matrix where rows represent groups and columns represent success/failure counts
}
\description{
This function creates a contingency table by aggregating counts based on a grouping variable.
}
\examples{
# Generate example data
set.seed(123)
data <- data.frame(
  dose = rep(c(0, 1, 10), each = 3),
  alive = c(9, 8, 10, 7, 6, 5, 3, 4, 2),
  dead = c(1, 2, 0, 3, 4, 5, 7, 6, 8)
)

# Create contingency table
create_contingency_table(data, "dose", "alive", "dead")

}
