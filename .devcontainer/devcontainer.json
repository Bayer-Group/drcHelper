{
  "postCreateCommand": "sudo apt update && sudo apt install -y r-base pandoc libharfbuzz-dev libfribidi-dev libfreetype6-dev libpng-dev libtiff5-dev libjpeg-dev libwebp-dev && sudo Rscript -e \"install.packages(c('rmarkdown', 'testthat', 'devtools'), repos='https://cloud.r-project.org/')\" && sudo Rscript -e \"devtools::install_local('.', dependencies=TRUE)\""
}
